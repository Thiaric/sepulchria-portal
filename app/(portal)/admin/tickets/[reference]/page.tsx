import { TicketLiveSync } from "@/components/support/ticket-live-sync";
import Link from "next/link"; import { notFound } from "next/navigation"; import { requireStaff } from "@/lib/auth/require-staff"; import { createAdminClient } from "@/lib/supabase/admin"; import { assignTicketToSelf, staffTicketMessage, updateTicketState } from "../actions";
function fmt(v:string){return new Intl.DateTimeFormat("en-GB",{dateStyle:"medium",timeStyle:"short"}).format(new Date(v));}
export default async function AdminTicketPage({params}:{params:Promise<{reference:string}>}){const staff=await requireStaff();const {reference}=await params;const admin=createAdminClient();const {data:t,error}=await admin.from("tickets").select("id,public_reference,category,status,priority,subject,opened_by_user_id,opened_by_character_id,assigned_staff_user_id,created_at").eq("public_reference",reference).maybeSingle();if(error)throw new Error(error.message);if(!t)notFound();const {data:m,error:me}=await admin.from("ticket_messages").select("id,author_user_id,visibility,body,created_at").eq("ticket_id",t.id).order("created_at",{ascending:true});if(me)throw new Error(me.message);return <main className="p-5 sm:p-7 lg:p-9"><TicketLiveSync reference={t.public_reference} admin/><div className="mx-auto max-w-5xl"><Link href="/admin/tickets" className="text-[8px] uppercase text-[rgb(var(--sep-colour-a58b68))]">← Ticket Queue</Link><div className="mt-7 border border-[rgb(var(--sep-colour-60482e))]/45 bg-[rgb(var(--sep-colour-15100d))] p-6"><p className="text-[8px] uppercase text-[rgb(var(--sep-colour-8c704b))]">{t.public_reference} · {t.category}</p><h1 className="mt-2 font-serif text-3xl text-[rgb(var(--sep-colour-ead5ac))]">{t.subject}</h1><p className="mt-3 text-[9px] text-[rgb(var(--sep-colour-756957))]">Opened {fmt(t.created_at)}</p><div className="mt-5 flex flex-wrap gap-3">{t.assigned_staff_user_id!==staff.userId?<form action={assignTicketToSelf}><input type="hidden" name="ticketId" value={t.id}/><button className="border border-[rgb(var(--sep-colour-80613b))] px-4 py-2 text-[8px] uppercase">Assign to Me</button></form>:null}<form action={updateTicketState} className="flex gap-2"><input type="hidden" name="ticketId" value={t.id}/><select key={`status-${t.status}`} name="status" defaultValue={t.status} className="bg-[rgb(var(--sep-colour-100c09))] px-2"><option value="open">Open</option><option value="in_progress">In Progress</option><option value="waiting_on_player">Waiting on Player</option><option value="waiting_on_staff">Waiting on Staff</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select><select key={`priority-${t.priority}`} name="priority" defaultValue={t.priority} className="bg-[rgb(var(--sep-colour-100c09))] px-2"><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select><button className="border border-[rgb(var(--sep-colour-80613b))] px-4 py-2 text-[8px] uppercase">Update</button></form></div></div><div className="mt-5 space-y-3">{(m??[]).map(x=><div key={x.id} className={`border p-5 ${x.visibility==="internal"?"border-dashed border-[rgb(var(--sep-colour-9a7749))]/65":"border-[rgb(var(--sep-colour-60482e))]/45"}`}><div className="text-[8px] uppercase text-[rgb(var(--sep-colour-8c704b))]">{x.visibility==="internal"?"Internal Staff Note":x.author_user_id===t.opened_by_user_id?"Player":"Staff"} · {fmt(x.created_at)}</div><p className="mt-3 whitespace-pre-wrap text-sm leading-7">{x.body}</p></div>)}</div>{t.status!=="closed"?<div className="mt-6 grid gap-4 lg:grid-cols-2">{[false,true].map(internal=><form key={String(internal)} action={staffTicketMessage} className="border border-[rgb(var(--sep-colour-60482e))]/45 p-5"><input type="hidden" name="ticketId" value={t.id}/><input type="hidden" name="internal" value={String(internal)}/><div className="text-[8px] uppercase">{internal?"Internal Staff Note":"Reply to Player"}</div><textarea name="body" required maxLength={10000} rows={7} className="mt-3 w-full bg-[rgb(var(--sep-colour-100c09))] p-3"/><button className="mt-3 border border-[rgb(var(--sep-colour-80613b))] px-4 py-2 text-[8px] uppercase">{internal?"Add Internal Note":"Send Reply"}</button></form>)}</div>:null}</div></main>}
