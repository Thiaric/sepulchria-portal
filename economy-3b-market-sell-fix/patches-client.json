[
  {
    "label": "sell action import",
    "old": "import {\n  buyMarketListing,\n} from \"@/app/(portal)/market/actions\";\n",
    "new": "import {\n  buyMarketListing,\n  sellMarketListing,\n} from \"@/app/(portal)/market/actions\";\n"
  },
  {
    "label": "listing sellable type",
    "old": "  stock_mode: \"finite\" | \"unlimited\";\n  stock_quantity: number | null;\n  item: {\n",
    "new": "  stock_mode: \"finite\" | \"unlimited\";\n  stock_quantity: number | null;\n  owned_sellable_quantity: number;\n  item: {\n"
  },
  {
    "label": "sell quantities state",
    "old": "  const [\n    pendingListingId,\n    setPendingListingId,\n  ] = useState<string | null>(\n    null,\n  );\n",
    "new": "  const [\n    sellQuantities,\n    setSellQuantities,\n  ] = useState<\n    Record<string, number>\n  >({});\n\n  const [\n    pendingListingId,\n    setPendingListingId,\n  ] = useState<string | null>(\n    null,\n  );\n\n  const [\n    pendingDirection,\n    setPendingDirection,\n  ] = useState<\n    \"buy\" | \"sell\" | null\n  >(null);\n"
  },
  {
    "label": "sell helper",
    "old": "  function buy(\n    listing:\n      MarketCatalogueListing,\n  ) {\n",
    "new": "  function sellQuantityFor(\n    listingId: string,\n  ) {\n    return sellQuantities[\n      listingId\n    ] ?? 1;\n  }\n\n  function buy(\n    listing:\n      MarketCatalogueListing,\n  ) {\n"
  },
  {
    "label": "buy pending direction",
    "old": "    setPendingListingId(\n      listing.id,\n    );\n\n    setFeedback((current) => {\n",
    "new": "    setPendingListingId(\n      listing.id,\n    );\n    setPendingDirection(\"buy\");\n\n    setFeedback((current) => {\n"
  },
  {
    "label": "buy clear direction",
    "old": "        setPendingListingId(\n          null,\n        );\n\n        if (result.ok) {\n",
    "new": "        setPendingListingId(\n          null,\n        );\n        setPendingDirection(null);\n\n        if (result.ok) {\n"
  },
  {
    "label": "sell function",
    "old": "  const compactInput =\n",
    "new": "  function sell(\n    listing:\n      MarketCatalogueListing,\n  ) {\n    if (pending) return;\n\n    const quantity =\n      sellQuantityFor(\n        listing.id,\n      );\n\n    setPendingListingId(\n      listing.id,\n    );\n    setPendingDirection(\"sell\");\n\n    setFeedback((current) => {\n      const next = {\n        ...current,\n      };\n\n      delete next[\n        listing.id\n      ];\n\n      return next;\n    });\n\n    startTransition(\n      async () => {\n        const result =\n          await sellMarketListing(\n            listing.id,\n            quantity,\n          );\n\n        setFeedback(\n          (current) => ({\n            ...current,\n            [listing.id]: {\n              ok: result.ok,\n              message:\n                result.message,\n            },\n          }),\n        );\n\n        setPendingListingId(\n          null,\n        );\n        setPendingDirection(null);\n\n        if (result.ok) {\n          setSellQuantities(\n            (current) => ({\n              ...current,\n              [listing.id]: 1,\n            }),\n          );\n\n          router.refresh();\n        }\n      },\n    );\n  }\n\n  const compactInput =\n"
  },
  {
    "label": "derive sell state",
    "old": "            const total =\n              listing.buy_price *\n              quantity;\n\n            const cannotAfford =\n",
    "new": "            const total =\n              listing.buy_price *\n              quantity;\n\n            const sellQuantity =\n              sellQuantityFor(\n                listing.id,\n              );\n\n            const sellTotal =\n              (listing.sell_price ??\n                0) *\n              sellQuantity;\n\n            const canSell =\n              listing.sell_price !==\n                null &&\n              listing.sell_price > 0 &&\n              listing\n                .owned_sellable_quantity >\n                0;\n\n            const exceedsOwned =\n              sellQuantity >\n              listing\n                .owned_sellable_quantity;\n\n            const cannotAfford =\n"
  },
  {
    "label": "buy pending label",
    "old": "                      {pending &&\n                      pendingListingId ===\n                        listing.id\n                        ? \"Buying...\"\n                        : \"Buy\"}\n",
    "new": "                      {pending &&\n                      pendingListingId ===\n                        listing.id &&\n                      pendingDirection ===\n                        \"buy\"\n                        ? \"Buying...\"\n                        : \"Buy\"}\n"
  }
]